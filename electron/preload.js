// Preload script for Electron security
// This script runs before the main app code and can safely expose limited APIs

// Currently empty but kept as best practice for future API exposure if needed
// All game code runs in the web context with full access to DOM and standard APIs
