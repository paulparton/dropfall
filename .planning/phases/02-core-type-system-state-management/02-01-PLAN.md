---
phase: 02-core-type-system-state-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/types/Entity.ts, src/types/Game.ts]
autonomous: true
requirements: [GAME-01, GAME-02, ARCH-01]
must_haves:
  truths:
    - "Entity type exists with lifecycle hooks (init, update, destroy)"
    - "Game state type covers arena bounds, entity maps, game modes"
    - "Both types export without 'any' type"
    - "TypeScript strict mode compilation passes (0 errors)"
  artifacts:
    - path: "src/types/Entity.ts"
      provides: "Base entity interface with lifecycle"
      exports: ["Entity", "EntityType", "EntityLifecycleHooks", "EntityState", "isEntity"]
    - path: "src/types/Game.ts"
      provides: "Game state and arena configuration types"
      exports: ["GameState", "GameMode", "ArenaBounds", "GamePhase", "EntityMap"]
  key_links:
    - from: "src/types/Entity.ts"
      to: "tsconfig.json"
      via: "strict mode compliance"
      pattern: "no implicit any"
    - from: "src/types/Game.ts"
      to: "src/entities/Arena.js"
      via: "reusable types"
      pattern: "ArenaBounds matches existing arena dimensions"
---

<objective>
Create foundational type definitions for Entity base class and Game state. These are the core types that all subsequent domain types (Physics, Audio, Input) will reference.

Purpose: Establish the type foundation for entity lifecycle system and game state management
Output: Two .ts files with no `any` types, full TypeScript strict mode compliance
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-type-system-state-management/02-CONTEXT.md

# Phase 1 Output (TypeScript ready)
@.planning/phases/01-typescript-foundation/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Key types the executor will need from existing code -->

From tsconfig.json (Phase 1):
- strict: true (all strict flags enabled)
- allowJs: true (JS files readable but not enforced)
- target: ES2020
- module: ESNext

From src/entities/Arena.js (existing entity to understand):
- Props: position {x, y}, size {width, height}, bounds
- Methods: initialize(), update(), destroy()
- Properties: id, type='arena', health-like properties

From src/entities/Player.js (existing entity):
- Lifecycle: created → active → destroyed
- Properties: id, type='player', position {x,y}, velocity {x,y}, health
- Methods: initialize(context), update(dt), destroy(), handleInput()
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create src/types/Entity.ts with lifecycle interface</name>
  <files>src/types/Entity.ts</files>
  <read_first>
    - src/entities/Player.js (understand current lifecycle)
    - src/entities/Arena.js (understand existing entity patterns)
    - tsconfig.json (verify strict mode settings)
  </read_first>
  <action>
Create src/types/Entity.ts with the following structure. All exports must be at root level (not namespaced).

**Exports (required):**
1. EntityType: string literal union = 'player' | 'arena' | 'effect' | 'particle' | 'lightning' | 'shockwave'

2. EntityState: string literal union = 'created' | 'ready' | 'active' | 'destroyed'

3. EntityLifecycleHooks: interface with optional methods:
   - initialize(context: GameContext): Promise<void>
   - update(deltaTime: number, context: GameContext): void
   - destroy(): Promise<void>
   - onCollision?(event: CollisionEvent): void

4. Entity: interface extending EntityLifecycleHooks with properties:
   - id: string (required)
   - type: EntityType (required)
   - state: EntityState (initially 'created')
   - position: { x: number; y: number } (required)
   - metadata: Record<string, unknown> (optional, for extensibility)

5. GameContext: interface (placeholder for Phase 3 integration) with:
   - deltaTime: number
   - gameState: unknown (will become GameState in future phase)
   - physics: unknown
   - audio: unknown

6. Type guard function:
   - isEntity(obj: unknown): obj is Entity
   - Checks for required properties: id (string), type (EntityType discrimination), state (EntityState discrimination)

7. Type refinements (discriminated unions):
   - PlayerEntity extends Entity { type: 'player'; health: number; velocity: { x: number; y: number } }
   - ArenaEntity extends Entity { type: 'arena'; bounds: { width: number; height: number } }

**Rules:**
- NO implicit any types
- Use `strict` TypeScript (verify tsconfig.json has strict: true)
- JSDoc comments on each export explaining purpose
- Keep GameContext minimal (Game state type comes next in Task 2)
  </action>
  <verify>
    <automated>npx tsc --noEmit src/types/Entity.ts 2>&1 | grep -E "error|^$" | tail -1</automated>
  </verify>
  <done>
    - Entity.ts exists in src/types/
    - All 7 exports present and exported at root
    - No implicit any types (strict mode check passes)
    - tsc --noEmit returns 0 errors
    - Discriminated union types work (PlayerEntity, ArenaEntity)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create src/types/Game.ts with game state interface</name>
  <files>src/types/Game.ts</files>
  <read_first>
    - src/types/Entity.ts (reference Entity types just created)
    - src/store.js (see current game state structure: gameMode, players, round, score)
    - src/main.js (see how game state is initialized and used)
  </read_first>
  <action>
Create src/types/Game.ts with the following structure. Import Entity types from Entity.ts.

**Exports (required):**
1. GameMode: string literal union = '1P' | '2P' | 'AI' (for future expansion)

2. Difficulty: string literal union = 'easy' | 'normal' | 'hard'

3. GamePhase: string literal union = 'menu' | 'loading' | 'playing' | 'paused' | 'roundEnd' | 'gameEnd'

4. ArenaBounds: interface
   - width: number
   - height: number
   - minX: number
   - maxX: number
   - minY: number
   - maxY: number
   - Comment: "Matches Arena.js 1000x600 default"

5. EntityMap: type = Record<string, Entity>
   Comment: "entities[id] = entity, e.g. entities['player-1']"

6. GameState: interface with:
   - id: string (game session ID)
   - mode: GameMode
   - phase: GamePhase
   - difficulty?: Difficulty (optional, only for 1P/AI modes)
   - players: Entity[] (1-2 Player entities)
   - arena: Entity (Arena entity)
   - entities: EntityMap (all active entities by ID)
   - round: number
   - score: { p1: number; p2: number }
   - isOnline: boolean
   - createdAt: Date
   - updatedAt: Date

7. GameContext type (finalized from Entity.ts placeholder):
   - gameState: GameState
   - deltaTime: number
   - physics: { raycast: Function; position: Function } (placeholder for Physics system)
   - audio: { play: Function; stop: Function } (placeholder for Audio system)
   - input: { getMovement: Function } (placeholder for Input system)

8. Type guard function:
   - isValidGameState(obj: unknown): obj is GameState
   - Checks: id (string), mode (GameMode discrimination), phase (GamePhase discrimination)

9. Factory function (optional but recommended):
   - createGameState(mode: GameMode, difficulty?: Difficulty): GameState
   - Returns a new GameState with initialized values
   - Sets phase='loading', round=0, score={p1:0, p2:0}, players=[], entities={}

10. ArenaBounds constant:
    - export const DEFAULT_ARENA_BOUNDS: ArenaBounds = { width: 1000, height: 600, minX: 0, maxX: 1000, minY: 0, maxY: 600 }

**Rules:**
- NO implicit any types
- Import Entity, GameContext from Entity.ts (not duplicating definitions)
- Use `import type` for type-only imports
- JSDoc on exports explaining relationship to Arena.js and Player.js
  </action>
  <verify>
    <automated>npx tsc --noEmit src/types/Game.ts 2>&1 | grep -E "error|^$" | tail -1</automated>
  </verify>
  <done>
    - Game.ts exists in src/types/
    - All 10 exports present and exported at root
    - Imports from Entity.ts work (no missing references)
    - No implicit any types (strict mode check passes)
    - tsc --noEmit returns 0 errors
    - DEFAULT_ARENA_BOUNDS constant matches Arena.js expectations (1000x600)
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Check that both files compile without errors:
   ```bash
   npx tsc --noEmit src/types/Entity.ts src/types/Game.ts
   ```
   Expected: No output (0 errors)

2. Verify imports structure by checking Entity.ts is importable:
   ```bash
   npx tsc --noEmit --skipLibCheck -c src/types/Game.ts && echo "Imports OK"
   ```

3. Verify no implicit any by looking for 'any' in generated definitions:
   ```bash
   grep -i "any" src/types/Entity.ts src/types/Game.ts
   ```
   Expected: No matches (or only in comments)

4. Type check from consumer perspective (store.js importing these):
   ```bash
   cat > /tmp/test-import.ts << 'EOF'
   import type { Entity, GameState } from './src/types/Game';
   const e: Entity = null as any; // Just checking import resolution
   const g: GameState = null as any;
   EOF
   npx tsc --noEmit /tmp/test-import.ts
   ```
   Expected: No errors (imports resolve)
</verification>

<success_criteria>

- ✓ src/types/Entity.ts exists with 7+ exports (Entity, EntityType, EntityState, EntityLifecycleHooks, GameContext, isEntity, type refinements)
- ✓ src/types/Game.ts exists with 10+ exports (GameState, GameMode, GamePhase, ArenaBounds, EntityMap, GameContext, isValidGameState, createGameState, DEFAULT_ARENA_BOUNDS)
- ✓ Both files pass `npm run type-check` with 0 errors
- ✓ No implicit `any` types in either file
- ✓ Strict mode TypeScript compiles both files successfully
- ✓ Exports are at root level (not namespaced)
- ✓ GameContext updated to include full system references (placeholder functions for Phase 3)
- ✓ ArenaBounds defaults match Arena.js (width: 1000, height: 600)

</success_criteria>

<output>
After completion, create `.planning/phases/02-core-type-system-state-management/02-01-SUMMARY.md`
</output>
