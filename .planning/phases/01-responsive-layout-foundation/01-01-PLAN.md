---
phase: "01-responsive-layout-foundation"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["index.html"]
autonomous: true
requirements: ["RL-04"]
user_setup: []
must_haves:
  truths:
    - "Viewport meta tag configured for mobile with proper initial-scale and width"
    - "iOS safe area support enabled via viewport-fit=cover"
  artifacts:
    - path: "index.html"
      provides: "Updated viewport meta tag"
      contains: "viewport-fit=cover"
  key_links:
    - from: "index.html"
      to: "browser viewport"
      via: "meta tag"
      pattern: "viewport.*content.*viewport-fit=cover"
---

<objective>
Update the viewport meta tag in index.html to properly support mobile devices with safe areas for iOS notched devices.

Purpose: Enable proper mobile rendering with correct viewport scaling and iOS safe area handling
Output: Updated index.html with proper viewport configuration
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/paulparton/Workbench/dropfall/index.html
@/Users/paulparton/Workbench/dropfall/.planning/ROADMAP.md
@/Users/paulparton/Workbench/dropfall/.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update viewport meta tag for mobile support</name>
  <files>index.html</files>
  <action>Update the viewport meta tag in index.html to properly support mobile devices. Current meta tag is:
  
  `<meta name="viewport" content="width=device-width, initial-scale=1.0" />`
  
  Update it to:
  
  `<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />`
  
  The `viewport-fit=cover` is essential for iOS devices with notch/home indicator to display content in the safe area regions. This addresses RL-04 requirement.
  
  Also add a comment explaining why viewport-fit=cover is needed for iOS safe areas.
  </action>
  <verify>
    <automated>grep -q 'viewport-fit=cover' index.html && echo "PASS: viewport-fit=cover found" || echo "FAIL: viewport-fit=cover missing"</automated>
  </verify>
  <done>Viewport meta tag updated with viewport-fit=cover; grep confirms the change exists in index.html</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client browser | User-controlled environment, viewport meta interpreted by browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | E (Elevation) | index.html meta tag | accept | Meta tags are low-risk, interpreted by browser only |
| T-01-02 | T (Tampering) | index.html | accept | Static file served to client, client can modify locally but doesn't affect server |
</threat_model>

<verification>
- [ ] Viewport meta tag contains `viewport-fit=cover`
- [ ] Meta tag format is valid HTML
- [ ] No regression to existing meta tags
</verification>

<success_criteria>
- `grep 'viewport-fit=cover' index.html` returns the updated meta tag
- HTML remains valid and parseable
- Requirement RL-04 marked as addressed
</success_criteria>

<output>
After completion, create `.planning/phases/01-responsive-layout-foundation/01-01-SUMMARY.md`
</output>
