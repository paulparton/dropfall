# 05-04: Effect Systems Migration

## Goal
Migrate effect systems (ParticleSystem, LightningSystem, ShockwaveSystem) to TypeScript.

## Context

**Existing:**
- `src/entities/ParticleSystem.js`
- `src/entities/LightningSystem.js`
- `src/entities/ShockwaveSystem.js`

**Required:**
- All three systems migrated to TypeScript
- Proper typing for particle pools, emission parameters

## Tasks

### 1. ParticleSystem.ts

```typescript
import * as THREE from 'three';
import { EntityBase } from './Entity.base';

export interface Particle {
  position: THREE.Vector3;
  velocity: THREE.Vector3;
  color: number;
  life: number;
  maxLife: number;
  size: number;
}

export class ParticleSystem extends EntityBase {
  type: 'particle' = 'particle';
  
  private particles: Particle[] = [];
  private geometry: THREE.BufferGeometry;
  private material: THREE.PointsMaterial;
  private mesh: THREE.Points;
  private maxParticles: number = 500;
  
  constructor() {
    super('particles', 'particle', { x: 0, y: 0 });
    // Initialize Three.js objects
  }
  
  emit(
    position: { x: number; y: number; z: number },
    velocity: { x: number; y: number; z: number },
    color: number,
    count: number = 1
  ): void {
    // Emit particles
  }
  
  update(delta: number, context: GameContext): void {
    // Update particle positions, lifetimes
    // Update geometry
  }
  
  cleanup(): void {
    // Dispose geometry, material
  }
}
```

### 2. LightningSystem.ts

```typescript
import * as THREE from 'three';
import { EntityBase } from './Entity.base';

export interface LightningBolt {
  start: THREE.Vector3;
  end: THREE.Vector3;
  segments: THREE.Vector3[];
  life: number;
  maxLife: number;
}

export class LightningSystem extends EntityBase {
  type: 'lightning' = 'lightning';
  
  private bolts: LightningBolt[] = [];
  private material: THREE.LineBasicMaterial;
  
  constructor() {
    super('lightning', 'lightning', { x: 0, y: 0 });
  }
  
  strike(start: THREE.Vector3, end: THREE.Vector3): void {
    // Create lightning bolt
  }
  
  update(delta: number, context: GameContext): void {
    // Update bolts, fade out
  }
  
  cleanup(): void {
    // Dispose
  }
}
```

### 3. ShockwaveSystem.ts

```typescript
import * as THREE from 'three';
import { EntityBase } from './Entity.base';

export interface Shockwave {
  center: THREE.Vector3;
  radius: number;
  maxRadius: number;
  strength: number;
  life: number;
}

export class ShockwaveSystem extends EntityBase {
  type: 'shockwave' = 'shockwave';
  
  private shockwaves: Shockwave[] = [];
  private geometry: THREE.RingGeometry;
  private material: THREE.MeshBasicMaterial;
  
  constructor() {
    super('shockwaves', 'shockwave', { x: 0, y: 0 });
  }
  
  emit(center: THREE.Vector3, strength: number): void {
    // Create expanding ring
  }
  
  update(delta: number, context: GameContext): void {
    // Expand rings, detect collisions
  }
  
  cleanup(): void {
    // Dispose
  }
}
```

### 4. Update entities/index.ts

```typescript
export { EntityBase, EntityEventEmitter, type EntityEvent, type EntityEventType } from './Entity.base';
export { Player, type PowerUpEffect } from './Player';
export { Arena, type ArenaTileData, type TileState } from './Arena';
export { ParticleSystem, type Particle } from './ParticleSystem';
export { LightningSystem, type LightningBolt } from './LightningSystem';
export { ShockwaveSystem, type Shockwave } from './ShockwaveSystem';
export * from '../types/Entity';
```

## Dependencies

- 05-01 (Entity.base.ts)

## Acceptance

- [ ] ParticleSystem.ts created
- [ ] LightningSystem.ts created
- [ ] ShockwaveSystem.ts created
- [ ] entities/index.ts exports all
- [ ] TypeScript strict mode passes
