---
phase: 01-typescript-foundation
plan: 03
type: auto
wave: 2
depends_on: ["01-01", "01-02"]
files_modified: ["src/main.js"]
autonomous: true
requirements:
  - TS-STRICT
  - TS-DEVSERVER

must_haves:
  truths:
    - "TypeScript type checking shows 0 errors for the existing src/ directory"
    - "IDE provides IntelliSense for JavaScript files as if they were TypeScript"
    - "Hot reload works when editing .js files in src/"
    - "Build completes with no TypeScript complaints about current codebase"
  artifacts:
    - path: "src/"
      provides: "All game source files type-checkable"
      contains: ["main.js", "*.js files"]
    - path: "tsconfig.json"
      provides: "TS configuration that accepts current .js files"
      contains: ["allowJs: true", "checkJs: false"]
---

# Phase 01.3: Type Checking & Verification

<objective>
Verify that TypeScript build pipeline fully operates on existing codebase. Enable type checking on existing .js files enough to catch errors, but not enforce full typing yet (checkJs: false). Confirm zero errors and that IDE integration works.

Purpose: Establish a clean TS build baseline before migrating first .js files to .ts in Phase 2. Ensure no hidden compilation errors lurk that will bite us during migration.

Output: Confirmed 0 TypeScript errors on all src/ files, IDE IntelliSense working, dev hot-reload operational with existing .js code.
</objective>

<execution_context>
@/home/paupl/Workbench/dropfall/.planning/phases/01-typescript-foundation/01-CONTEXT.md
</execution_context>

<context>
**Current state:**
- tsconfig.json created and configured (Plan 01-01)
- Vite build pipeline updated (Plan 01-02)
- All existing .js files still in src/, no TypeScript files yet
- checkJs: false means TS won't check .js file contents for type errors, but allowJs: true lets them compile

**Key setting:** `checkJs: false` — This prevents TypeScript from complaining about loose typing in existing .js. Once we migrate files to .ts, they'll inherit strict mode automatically.

**Goal validation:** When `npm run type-check` runs, it should report:
- "Found X files, 0 errors" (or similar success message)
- No blocking errors about module resolution or missing types
</context>

<tasks>

<task type="auto">
  <name>Run type checking and verify 0 errors on existing src/</name>
  <files></files>
  <read_first>
    - tsconfig.json (to verify checkJs is false)
    - package.json (to see type-check script)
  </read_first>
  <action>
Execute type checking on the current codebase to establish a zero-error baseline.

**Actions:**

1. **Run type check command:**
   ```bash
   cd /home/paupl/Workbench/dropfall
   npm run type-check
   ```

2. **Expected output:**
   - No errors related to module resolution (Three.js, Rapier3D, Zustand types must be available)
   - No errors on .js files (because checkJs: false)
   - Success message from tsc

3. **If errors appear:**
   - **Module not found errors** (e.g., "Cannot find module 'three'"): Run `npm install` again or check package.json has @types/three
   - **Type errors in library code**: This shouldn't happen because skipLibCheck: false + @types packages should have correct definitions
   - **Other errors**: Document and fail the task (requires fixing)

4. **Verification success = 0 TypeScript errors** (not counting Node/browser compatibility warnings)

**Note:** This task validates the infrastructure. If it fails, Phase 1 itself fails and cannot proceed to Phase 2. The build pipeline must be clean before we start migrating code.
  </action>
  <verify>
    <automated>npm run type-check 2>&1 | grep -q "0 errors" || npm run type-check 2>&1 | grep -q "Program is valid" || npm run type-check 2>&1 | grep -qv "error" && echo "✓ Type checking passes"</automated>
  </verify>
  <done>
    - npm run type-check executes successfully
    - 0 TypeScript errors reported
    - All project dependencies (Three.js, Rapier3D, etc.) have valid type definitions
    - Ready for developers to start migrating .js files to .ts in Phase 2
  </done>
</task>

<task type="auto">
  <name>Verify IDE TypeScript support and hot reload functionality</name>
  <files></files>
  <read_first>
    - vite.config.js (dev server config)
    - tsconfig.json (TypeScript settings)
  </read_first>
  <action>
Perform manual verification that IDE integration and dev hot-reload work correctly (these are user-facing features that can't be fully automated).

**Verification steps:**

1. **Open VS Code** (or user's IDE)
   - Open the project folder `/home/paupl/Workbench/dropfall`
   - Verify VS Code recognizes it as a TypeScript project (check status bar or Ctrl+Shift+P → "TypeScript: Restart TS Server")

2. **Test IDE IntelliSense:**
   - Open src/main.js
   - Position cursor on a function call (e.g., `useGameStore()` or `initRenderer()`)
   - Hover — should show type information (if available) or at least function signature
   - Type `import { ` and verify autocomplete suggests exported items from other files
   - Type `const player = new ` and verify suggestions appear

3. **Test dev server hot reload:**
   - Run `npm run dev` in terminal
   - Open http://localhost:5173 in browser
   - Edit a .js file in src/ (e.g., add a console.log statement)
   - Save and verify browser refreshes automatically (or at least dev server shows recompile)
   - Game should still work (no broken state)

4. **Record observations:**
   - IDE responds quickly (IntelliSense appears within 1s)
   - No TypeScript errors shown (red squiggles shouldn't appear on valid code)
   - Hot reload triggers within 2-3s of save
   - Browser shows no errors in console (existing game still functions)

**Result:** If all checks pass, TypeScript foundation is solid. If not, document issues (e.g., "IDE slow", "hot reload not firing") for troubleshooting post-Phase-1.

**Note:** This is a manual verification task. Automated checks can verify dev server runs, but IDE behavior requires user interaction.
  </action>
  <verify>
    <automated>npm run dev > /tmp/dev.log 2>&1 &
DEV_PID=$!
sleep 3
kill $DEV_PID 2>/dev/null
grep -q "ready" /tmp/dev.log && echo "✓ Dev server started"</automated>
  </verify>
  <done>
    - IDE (VS Code) recognizes TypeScript project
    - IntelliSense works on .js files (basic function/variable suggestions)
    - npm run dev starts dev server successfully
    - Hot reload responds to file changes
    - No blocking errors in IDE or dev console
    - TypeScript foundation is operational and ready for Phase 2 migration
  </done>
</task>

</tasks>

<verification>
1. `npm run type-check` reports 0 errors
2. `npm run build` completes with no TypeScript errors
3. `npm run dev` starts dev server and hot-reload works
4. IDE shows no TypeScript errors on existing .js files in src/
5. No performance regression compared to baseline (from Plan 01-02)
</verification>

<success_criteria>
- ✓ Type checking completes with 0 errors
- ✓ All project dependencies have valid type definitions
- ✓ IDE integration functional (IntelliSense shows suggestions)
- ✓ Dev server hot-reload works with .js files
- ✓ Build pipeline clean and ready for Phase 2 migration work
- ✓ Baseline performance maintained (no regression)
</success_criteria>

<output>
After completion, create `.planning/phases/01-typescript-foundation/01-03-SUMMARY.md`
</output>
