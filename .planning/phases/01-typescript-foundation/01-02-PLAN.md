---
phase: 01-typescript-foundation
plan: 02
type: auto
wave: 1
depends_on: ["01-01"]
files_modified: ["vite.config.js", "src/style.css"]
autonomous: true
requirements:
  - TS-BUILD
  - TS-DEVSERVER
  - TS-PERF

must_haves:
  truths:
    - "Vite dev server recognizes and compiles .ts files with hot reload"
    - "npm run build uses TypeScript type checking"
    - "Incremental compilation faster than full rebuild on code changes"
    - "Bundle chunks remain separated (Three.js, Rapier3D, main code)"
  artifacts:
    - path: "vite.config.js"
      provides: "Vite configuration with TypeScript support"
      contains: ["optimizeDeps", "include: ['three', '@dimforge/rapier3d-compat']"]
  key_links:
    - from: "vite.config.js"
      to: "tsconfig.json"
      via: "Vite reads tsconfig for TS compilation options"
      pattern: "tsconfig"
---

# Phase 01.2: Vite Build Pipeline Integration

<objective>
Ensure Vite build pipeline recognizes and properly handles TypeScript files. Verify no performance regression from current baseline. Enable incremental TypeScript compilation for faster dev iteration.

Purpose: Complete the build infrastructure so TypeScript code compiles efficiently in both dev and production modes.

Output: Updated vite.config.js with TS support enabled, verified build performance, dev server hot-reload working with .ts files.
</objective>

<execution_context>
@/home/paupl/Workbench/dropfall/.planning/phases/01-typescript-foundation/01-CONTEXT.md
</execution_context>

<context>
**Current state:**
- vite.config.js exists with manual chunks for Three.js and Rapier3D
- Dev server currently handles .js files
- Current baseline: build time ~2-5s, bundle size known (measure current before this task)

**Vite + TypeScript facts:**
- Vite automatically uses tsconfig.json if it exists (no explicit plugin needed)
- Only need to ensure tsconfig.json is present and valid (from Plan 01-01)
- Performance.measureCls — ensure optimizeDeps includes the heavy libraries we're splitting
</context>

<tasks>

<task type="auto">
  <name>Verify vite.config.js includes TypeScript-aware settings</name>
  <files>vite.config.js</files>
  <read_first>
    - vite.config.js (current configuration)
    - tsconfig.json (to verify Vite will read it)
  </read_first>
  <action>
Update `vite.config.js` to ensure TypeScript awareness. Vite will automatically detect tsconfig.json and compile .ts files, but explicitly verify the following settings are present or added:

1. **Optimize deps (pre-bundled dependencies):** Keep current manual chunks
   - Ensure Three.js and Rapier3D remain in optimizeDeps.include
   - These are heavy libraries, pre-bundling them improves startup time

2. **Current settings (from existing vite.config.js):**
   ```javascript
   optimizeDeps: {
     include: ['three', '@dimforge/rapier3d-compat', 'zustand'],
   }
   ```

3. **Build output config:** Keep existing manualChunks but add a comment noting TypeScript support is enabled via tsconfig.json

4. **Server config:** Keep existing (port 5173, host 0.0.0.0)

**Action:** If vite.config.js doesn't already mention tsconfig awareness, add a comment like:
```javascript
// TypeScript support enabled via tsconfig.json (strict mode)
// Vite auto-discovers and respects tsconfig.json compiler options
```

**Result:** No code changes needed if vite.config.js is already correct. Just verify the include list and add comment.
  </action>
  <verify>
    <automated>grep -q "optimizeDeps" vite.config.js && grep -q "three" vite.config.js && grep -q "rapier" vite.config.js && echo "✓ vite.config.js ready for TypeScript"</automated>
  </verify>
  <done>
    - vite.config.js verified to include optimizeDeps with Three.js and Rapier3D
    - Comment added noting TypeScript is enabled via tsconfig.json
    - No breaking changes to existing build configuration
  </done>
</task>

<task type="auto">
  <name>Measure and verify build performance baseline</name>
  <files></files>
  <read_first>
    - vite.config.js (build optimization settings)
    - package.json (build scripts)
  </read_first>
  <action>
Run baseline performance tests to ensure no regression after TypeScript setup.

**Actions (in order):**

1. **Measure current state** (before any TypeScript .ts files exist):
   ```bash
   cd /home/paupl/Workbench/dropfall
   npm install  # Install TypeScript, ESLint packages from Plan 01-01
   npm run build 2>&1 | tee build-baseline.log
   ```
   Record:
   - Build completion time
   - Final bundle size (check dist/ folder)
   - Any errors or warnings

2. **Measure dev server startup:**
   ```bash
   time npm run dev  # Ctrl+C after "Local: http://..."
   ```
   Record startup time

3. **Create a baseline record** (document in terminal output for reference):
   - Build time: [X seconds]
   - Main bundle size: [Y KB]
   - Three.js chunk size: [Z KB]
   - Rapier3D chunk size: [W KB]
   - Dev startup time: [V seconds]

4. **Commit baseline results:**
   After Phase 01 execution, compare actual results against recorded baseline. If regression > 5%, investigate Vite/TS config.

**Note:** This is a data-collection task. Actual comparison of before/after happens during execute-phase report.
  </action>
  <verify>
    <automated>npm run build > /dev/null 2>&1 && echo "✓ Build executes successfully" && npm run dev > /dev/null 2>&1 &
PID=$!
sleep 2
kill $PID 2>/dev/null
echo "✓ Dev server starts successfully"</automated>
  </verify>
  <done>
    - npm install completes successfully
    - npm run build completes with 0 errors
    - npm run dev starts without errors
    - Baseline metrics documented for comparison post-migration
    - No performance regression vs current (recorded baseline available for future phases)
  </done>
</task>

</tasks>

<verification>
1. Run `npm install` — all new TypeScript and ESLint packages fetched
2. Run `npm run build` — should complete with no new errors
3. Run `npm run dev` — should start dev server normally
4. Check dist/ folder exists and contains build output
5. Verify build time is similar to recorded baseline (within 5%)
</verification>

<success_criteria>
- ✓ npm install succeeds with all TypeScript + ESLint packages
- ✓ npm run build produces valid dist/ output with no TS errors
- ✓ npm run dev starts dev server successfully
- ✓ Bundle size within 5% of baseline (no regression)
- ✓ Build time within 5% of baseline (no significant slowdown)
</success_criteria>

<output>
After completion, create `.planning/phases/01-typescript-foundation/01-02-SUMMARY.md`
</output>
