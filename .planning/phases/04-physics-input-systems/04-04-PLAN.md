---
phase: 04-physics-input-systems
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/systems/PhysicsSystem.ts
  - src/handlers/InputHandler.ts
  - src/main.js
  - src/entities/Player.js
  - src/entities/Arena.js
  - src/input.js
  - src/physics.js
autonomous: true
requirements:
  - REQ-PHYSICS-01
  - REQ-PHYSICS-02
  - REQ-INPUT-01
  - REQ-INPUT-02
  - REQ-INPUT-03
  - REQ-VALIDATION-01
  - REQ-TEST-01

gap_closure: true

must_haves:
  truths:
    - "Physics events (collision, knockback, out-of-bounds) are emitted to subscribers"
    - "Game logic receives events without direct physics queries"
    - "InputHandler normalizes keyboard, gamepad, and AI input to InputPayload"
    - "Input payloads validated before processing"
    - "Physics events validated on emission"
    - "Tests exist and pass with ≥80% coverage on Input and Physics systems"
  artifacts:
    - path: "src/systems/PhysicsSystem.ts"
      provides: "Physics events (collision, knockback, out-of-bounds) actually emitted"
    - path: "src/handlers/InputHandler.ts"
      provides: "Input validation wired, used by game code"
    - path: "src/main.js"
      provides: "Uses PhysicsSystem and InputHandler instead of physics.js/input.js"
    - path: "src/validation/schemas.ts"
      provides: "Validation schemas (already exists)"
  key_links:
    - from: "src/main.js"
      to: "src/systems/PhysicsSystem.ts"
      via: "imports and subscribes to events"
    - from: "src/main.js"
      to: "src/handlers/InputHandler.ts"
      via: "imports and uses InputHandler"
    - from: "src/systems/PhysicsSystem.ts"
      to: "src/validation/schemas.ts"
      via: "calls validatePhysicsEvent()"
    - from: "src/handlers/InputHandler.ts"
      to: "src/validation/schemas.ts"
      via: "calls validateInputPayload()"
---

<objective>
Close verification gaps from 04-VERIFICATION.md:

1. Implement actual collision event emission using Rapier's contact events API
2. Wire PhysicsSystem to game code (main.js, Player.js, Arena.js)
3. Wire InputHandler to game code (main.js)
4. Wire input validation in InputHandler (schemas exist but not used)
5. Wire physics validation in PhysicsSystem (schemas exist but not used)
6. Add tests to achieve ≥80% coverage on InputHandler

Purpose: Complete the integration work that was left incomplete in plans 04-01, 04-02, 04-03
Output: All 6 gaps from VERIFICATION.md resolved
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-physics-input-systems/04-CONTEXT.md
@.planning/phases/04-physics-input-systems/04-VERIFICATION.md
@.planning/phases/04-physics-input-systems/04-01-PLAN.md
@.planning/phases/04-physics-input-systems/04-02-PLAN.md

# Gap sources from VERIFICATION.md
@src/systems/PhysicsSystem.ts
@src/handlers/InputHandler.ts
@src/main.js
@src/entities/Player.js
@src/entities/Arena.js
@src/input.js
@src/physics.js
@src/validation/schemas.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement actual collision event emission in PhysicsSystem</name>
  <files>src/systems/PhysicsSystem.ts</files>
  <behavior>
    - Test: emitCollisionEvents() calls this.emit('collision', collisionEvent) with real data
    - Test: Collision events include entityA, entityB, started, impulse fields
    - Test: Rapier contact events are processed each physics step
  </behavior>
  <action>
Fix the stub at emitCollisionEvents() (lines 461-484):

1. Import Rapier's contact event types and event queue API
2. Enable collision events on collider creation:
   - Add eventQueue to world step options
   - Set collider desc with .setActiveEvents(RAPIER.ActiveEvents.COLLISION_EVENTS)
3. In emitCollisionEvents():
   - Drain the event queue: eventQueue.drainCollisionEvents((handle1, handle2, started) => {...})
   - Map handles to entity IDs using the bodies Map
   - Create CollisionEvent objects with: type='collision', entityA, entityB, started, impulse
   - CALL this.emit('collision', collisionEvent) - THIS WAS MISSING

Reference: https://rapier.rs/javascript/classes/World.html#step - eventQueue option
  </action>
  <verify>
<automated>grep -A 5 "this.emit.*collision" src/systems/PhysicsSystem.ts</automated>
  </verify>
  <done>Collision events are emitted to subscribers with real Rapier collision data</done>
</task>

<task type="auto">
  <name>Task 2: Wire PhysicsSystem to game code (main.js, Player.js, Arena.js)</name>
  <files>src/main.js, src/entities/Player.js, src/entities/Arena.js</files>
  <action>
Refactor game code to use PhysicsSystem instead of physics.js:

1. In src/main.js:
   - Import PhysicsSystem: import { PhysicsSystem } from './systems/PhysicsSystem'
   - Create physicsSystem instance alongside existing physics
   - Subscribe to physics events: physicsSystem.on('collision', handleCollision), physicsSystem.on('knockback', handleKnockback), physicsSystem.on('out-of-bounds', handleOutOfBounds)
   - Replace physics.js function calls with physicsSystem method calls
   - Keep physics.js for backward compatibility during transition (can remove after testing)

2. In src/entities/Player.js:
   - Replace import { world, createPlayerBody } from '../physics' with PhysicsSystem
   - Use physicsSystem.createBody(), physicsSystem.applyForce() instead
   - Subscribe to collision events for damage/knockback

3. In src/entities/Arena.js:
   - Replace import { createTileBody, world } from '../physics' with PhysicsSystem
   - Use physicsSystem for tile body creation

Do NOT delete physics.js yet - mark as deprecated with comment for later cleanup.
  </action>
  <verify>
<automated>grep -l "PhysicsSystem" src/main.js && grep -l "physicsSystem.on" src/main.js</automated>
  </verify>
  <done>Game code (main.js, Player.js, Arena.js) imports and uses PhysicsSystem</done>
</task>

<task type="auto">
  <name>Task 3: Wire InputHandler to game code (main.js)</name>
  <files>src/main.js, src/input.js</files>
  <action>
Refactor main.js to use InputHandler instead of input.js:

1. In src/main.js:
   - Import InputHandler: import { InputHandler } from './handlers/InputHandler'
   - Create inputHandler instance
   - Replace getPlayer1Input() / getPlayer2Input() calls with inputHandler polling
   - Use inputHandler.getLastInput() for player input

2. Keep input.js for backward compatibility during transition (can remove after testing)

The InputHandler should provide the same functionality as input.js but with:
   - Unified interface for all input sources
   - Event-based subscription if needed
   - Same key mappings and gamepad thresholds
  </action>
  <verify>
<automated>grep -l "InputHandler" src/main.js && grep -l "inputHandler" src/main.js</automated>
  </verify>
  <done>main.js imports and uses InputHandler for player input</done>
</task>

<task type="auto">
  <name>Task 4: Wire input validation in InputHandler</name>
  <files>src/handlers/InputHandler.ts</files>
  <action>
Add validation to InputHandler using existing schemas:

1. Import validation schemas from src/validation/schemas.ts:
   - import { validateInputPayload } from '../validation/schemas'
   
2. In the poll() method or dispatch() method:
   - Call validateInputPayload(inputPayload) BEFORE dispatching to subscribers
   - If validation fails, log warning and skip invalid input
   
3. If the schema validation throws, catch and handle gracefully (don't crash)

The schemas already exist - just wire them up.
  </action>
  <verify>
<automated>grep -l "validateInputPayload" src/handlers/InputHandler.ts</automated>
  </verify>
  <done>InputHandler validates payloads before processing</done>
</task>

<task type="auto">
  <name>Task 5: Wire physics validation in PhysicsSystem</name>
  <files>src/systems/PhysicsSystem.ts</files>
  <action>
Add validation to PhysicsSystem event emission:

1. Import validation schemas from src/validation/schemas.ts:
   - import { validatePhysicsEvent } from '../validation/schemas'
   
2. In each emit method (emitCollisionEvents, applyKnockback, emitOutOfBoundsEvents):
   - Create the event object
   - Call validatePhysicsEvent(event) BEFORE calling this.emit()
   - If validation fails, skip emission and log warning

The schemas already exist - just wire them up.
  </action>
<verify>
<automated>grep -l "validatePhysicsEvent" src/systems/PhysicsSystem.ts</automated>
  </verify>
  <done>PhysicsSystem validates events before emission</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Add tests to achieve ≥80% coverage on InputHandler</name>
  <files>tests/input.test.ts</files>
  <behavior>
    - Test: Keyboard input handling (keydown/keyup events create KeyboardInput)
    - Test: Gamepad polling detects connected gamepad and creates GamepadInput
    - Test: AI integration - AIController output is normalized to AIInput
    - Test: Source priority - gamepad > keyboard > AI
    - Test: Input validation rejects invalid payloads
  </behavior>
  <action>
Add tests to achieve ≥80% coverage on InputHandler (currently 56.2%):

1. Add keyboard event handling tests:
   - Simulate keydown/keyup for WASD/Arrow keys
   - Verify KeyboardInput is created with correct flags
   - Test multiple simultaneous keys

2. Add gamepad polling tests:
   - Mock navigator.getGamepads() with test gamepad data
   - Test dead zone filtering (0.15 threshold)
   - Test analog stick values

3. Add AI integration tests:
   - Test that AIController output is wrapped as AIInput
   - Test difficulty switching (easy/normal/hard)

4. Add source priority tests:
   - Test gamepad takes priority over keyboard when connected
   - Test keyboard takes priority over AI when gamepad not connected

5. Add validation tests:
   - Test invalid payload is rejected
   - Test valid payload passes through
  </action>
  <verify>
<automated>npm test -- --run --coverage --include='src/handlers/InputHandler.ts' 2>&1 | grep -E "InputHandler|statements"</automated>
  </verify>
  <done>InputHandler coverage ≥80%</done>
</task>

</tasks>

<verification>
- [ ] Collision events actually emitted (not stub)
- [ ] Game code uses PhysicsSystem (not physics.js directly)
- [ ] Game code uses InputHandler (not input.js directly)
- [ ] InputHandler validates payloads with schemas
- [ ] PhysicsSystem validates events with schemas
- [ ] InputHandler tests pass with ≥80% coverage
- [ ] All existing tests still pass
- [ ] TypeScript compiles with strict mode
</verification>

<success_criteria>
All 6 gaps from 04-VERIFICATION.md are closed:
1. ✓ Collision events emitted
2. ✓ PhysicsSystem wired to game
3. ✓ InputHandler wired to game  
4. ✓ Input validation wired
5. ✓ Physics validation wired
6. ✓ InputHandler coverage ≥80%
</success_criteria>

<output>
After completion, create `.planning/phases/04-physics-input-systems/04-04-SUMMARY.md`
</output>
