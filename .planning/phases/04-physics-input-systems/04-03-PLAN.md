---
phase: 04-physics-input-systems
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01-PLAN.md
  - 04-02-PLAN.md
files_modified:
  - src/validation/schemas.ts
  - src/systems/PhysicsSystem.ts (update)
  - src/handlers/InputHandler.ts (update)
  - tests/physics.test.ts
  - tests/input.test.ts
autonomous: true
requirements:
  - REQ-VALIDATION-01
  - REQ-TEST-01
  - REQ-PHYSICS-02
  - REQ-INPUT-01

must_haves:
  truths:
    - "Input payloads validated before processing"
    - "Physics events validated on emission"
    - "Tests cover input combinations and physics events"
    - "≥80% coverage on Input and Physics systems"
  artifacts:
    - path: "src/validation/schemas.ts"
      provides: "Input and physics validation schemas"
    - path: "tests/physics.test.ts"
      provides: "Physics system tests"
    - path: "tests/input.test.ts"
      provides: "Input handler tests"
  key_links:
    - from: "src/handlers/InputHandler.ts"
      to: "src/validation/schemas.ts"
      via: "validates input before dispatch"
    - from: "src/systems/PhysicsSystem.ts"
      to: "src/validation/schemas.ts"
      via: "validates events before emit"
---

<objective>
Add validation schemas for input payloads and physics events. Create comprehensive tests for both systems.

Purpose: Prevent invalid data from propagating, ensure reliability
Output: Updated schemas.ts, physics.test.ts, input.test.ts
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-physics-input-systems/04-CONTEXT.md

# Reference files
@src/validation/schemas.ts (existing from Phase 2)
@src/types/Input.ts
@src/types/Physics.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add input validation schemas</name>
  <files>src/validation/schemas.ts</files>
  <action>
Add Zod schemas to src/validation/schemas.ts:
- KeyboardInputSchema: source, up, down, left, right, boost, timestamp
- GamepadInputSchema: source, gamepadIndex, up, down, left, right, boost, analog, timestamp
- AIInputSchema: source, difficulty, targetPosition, up, down, left, right, boost, timestamp
- InputPayloadSchema: union of above

Use existing schema patterns from Phase 2 (AudioContextSchema, etc.).
  </action>
  <verify>
<automated>grep -l "KeyboardInputSchema\|GamepadInputSchema\|AIInputSchema" src/validation/schemas.ts</automated>
  </verify>
  <done>Input validation schemas created in schemas.ts</done>
</task>

<task type="auto">
  <name>Task 2: Add physics event validation schemas</name>
  <files>src/validation/schemas.ts</files>
  <action>
Add Zod schemas for physics events:
- CollisionEventSchema: type, entityA, entityB, started, impulse
- KnockbackEventSchema: type, targetEntity, force, duration
- OutOfBoundsEventSchema: type, entity, lastPosition, direction
- PhysicsEventSchema: union of above

Ensure schemas match PhysicsEvent types exactly.
  </action>
  <verify>
<automated>grep -l "CollisionEventSchema\|KnockbackEventSchema\|OutOfBoundsEventSchema" src/validation/schemas.ts</automated>
  </verify>
  <done>Physics event validation schemas created</done>
</task>

<task type="auto">
  <name>Task 3: Create input handler tests</name>
  <files>tests/input.test.ts</files>
  <action>
Create tests/input.test.ts with Vitest:
- Test keyboard input normalization
- Test gamepad input with dead zone
- Test input priority (gamepad > keyboard > AI)
- Test AI difficulty switching (easy, normal, hard)
- Test subscription/unsubscribe pattern
- Test getLastInput() returns correct payload
- Test destroy() cleans up listeners

Use mocks for keyboard events and gamepad API.
Reference Phase 2 test patterns.
  </action>
  <verify>
<automated>npm test -- tests/input.test.ts 2>/dev/null || echo "Test file needs npm test setup"</automated>
  </verify>
  <done>Input handler tests created, ≥80% coverage target</done>
</task>

<task type="auto">
  <name>Task 4: Create physics system tests</name>
  <files>tests/physics.test.ts</files>
  <action>
Create tests/physics.test.ts with Vitest:
- Test lifecycle: init → ready → stepping → disposed
- Test body creation and destruction
- Test force application
- Test event emission (collision, knockback, out-of-bounds)
- Test subscriber pattern
- Test state guards (throw if wrong state)

Mock Rapier3D for unit testing without physics runtime.
  </action>
  <verify>
<automated>npm test -- tests/physics.test.ts 2>/dev/null || echo "Test file needs npm test setup"</automated>
  </verify>
  <done>Physics system tests created, ≥80% coverage target</done>
</task>

</tasks>

<verification>
- [ ] schemas.ts updated with input and physics schemas
- [ ] input.test.ts exists with ≥10 test cases
- [ ] physics.test.ts exists with ≥10 test cases
- [ ] Tests pass: npm test
- [ ] Coverage ≥80% on Input and Physics systems
</verification>

<success_criteria>
All input payloads validated before processing. All physics events validated before emission. Tests pass and provide ≥80% coverage.

Observable: npm test passes with coverage report showing ≥80% on src/handlers/ and src/systems/PhysicsSystem.ts
</success_criteria>

<output>
After completion, create `.planning/phases/04-physics-input-systems/04-03-SUMMARY.md`
</output>
