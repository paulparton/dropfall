

* Limit the max arena size to 16
* The destruction rate currently treats the smallest number as the most and the largest number as the least, fix this and fix all sliders
* the powerup messages block the players view